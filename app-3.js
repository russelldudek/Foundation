const menu=document.querySelector('.menu'),mobile=document.querySelector('.mobile');menu.onclick=()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',!open);mobile.classList.toggle('open',!open)};mobile.querySelectorAll('a').forEach(a=>a.onclick=()=>{mobile.classList.remove('open');menu.setAttribute('aria-expanded','false')});const reveals=[...document.querySelectorAll('.reveal')];if(matchMedia('(prefers-reduced-motion:reduce)').matches)reveals.forEach(x=>x.classList.add('show'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');io.unobserve(e.target)}}),{threshold:.1});reveals.forEach(x=>io.observe(x))}setSignal('data');setScenario('scout');
